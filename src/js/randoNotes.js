/*

import { rando, makeLearningSlug } from "../js/helper.js";

/*
goToLearn = e => {
    // get the array of learning resources for each respective component
    const allLearning = this.props.data;
    // grab a random learning which will be the drill down resource
    const randoLearning = rando(allLearning).randoLearning.title;

    const singleRandoLearningSlug = makeLearningSlug(randoLearning);
    this.props.history.push(`/learning/${singleRandoLearningSlug}`);
	};

  goToAdd = e => {
    console.log(this.props.history.push(`/learning`));
	};*/

	/*
	
	
			const randoReview = rando(learnData.review);
		const randoWatch = rando(learnData.watch);
		const randoRead = rando(learnData.read);




<Fragment>
          <Learn
            title="Review"
            data={data.review}
            history={this.props.history}
          />
          <Learn title="Watch" data={data.watch} history={this.props.history} />
          <Learn title="Read" data={data.read} history={this.props.history} />
          {/* Return children with no wrapper for Grid! React.Fragment renders out to nothing! */}
				</Fragment >


* /
